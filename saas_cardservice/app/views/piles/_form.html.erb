<%= form_with(model: pile, local: true) do |form| %>
  <% if pile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pile.errors.count, "error") %> prohibited this pile from being saved:</h2>

      <ul>
        <% pile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
